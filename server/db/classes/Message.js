export class Message {
    constructor(senderId, senderUsername, receiverId, message) {
        this.senderId = senderId;
        this.senderUsername = senderUsername;
        this.receiverId = receiverId;
        this.message = message;
    }
}
